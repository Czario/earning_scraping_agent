# Company hints for this workspace — human-curated files only.
# The pipeline reads but never writes these files.
#
# One file per ticker: {TICKER}.md (uppercase)
# Example: AAPL.md, MSFT.md, GOOGL.md
#
# Hints are injected into every LLM extraction prompt for that ticker.
# Use them to fix recurring mislabels, scale issues, or column-selection errors.
